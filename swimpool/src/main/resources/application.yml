spring:
  application:
    name: swimpool
#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://localhost:3306/swim_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
#    username: swim_user
#    password: qwer1234!
  jpa:
    hibernate:
#      ddl-auto: update # Entity 의 멤버변수가 변하면 자동으로 데이터베이스의 테이블도 갱신해준다.
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: false
    properties:
      hibernate:
        format_sql: true
      dialect: org.hibernate.dialect.MySQL8InnoDBDialect
  mvc:
    throw-exception-if-no-handler-found: true # @ExceptionHandler 에서 예외처리를 찾아라
  web:
    resources:
      add-mappings: false # 404.html 출력 하지 마라

server:
#  port: 8092
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

#logging:
#  level:
#    root: INFO                # 전체 기본 로깅 레벨 (기본값)
#    org.springframework.web: DEBUG  # 스프링 웹 관련 로그 상세화
#    com.mjc813.swimpool: DEBUG # 특정 패키지(내 프로젝트) 로그 상세화


